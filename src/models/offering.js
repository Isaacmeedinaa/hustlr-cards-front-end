class Offering {
  constructor(id, title, description, offerType, cardId) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.offerType = offerType;
    this.cardId = cardId;
  }
}

export default Offering;
